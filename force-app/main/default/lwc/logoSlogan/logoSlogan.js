import { LightningElement } from 'lwc';
import SVG_LOGO from '@salesforce/resourceUrl/VolcanoDragonLogo';
export default class LogoSlogan extends LightningElement {
    svgURL = `${SVG_LOGO}#image0`
}